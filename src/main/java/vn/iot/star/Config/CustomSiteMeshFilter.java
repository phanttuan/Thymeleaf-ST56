package vn.iot.star.Config;

// Placeholder class: SiteMesh removed because it depends on javax.servlet.* (Spring Boot 3 uses jakarta.servlet.*).
public class CustomSiteMeshFilter {
    // No implementation.
}